pub fn score(x: Float, y: Float) -> Int {
  case x, y {
    x, y if x >=. -1.0 && y >=. -1.0 && y <=. 1.0 && x <=. 1.0 -> 10
    x, y if x >=. -5.0 && y >=. -5.0 && y <=. 5.0 && x <=. 5.0 -> 5
    x, y if x >=. -10.0 && y >=. -10.0 && y <=. 10.0 && x <=. 10.0 -> 1
    _, _ -> 0
  }
}
